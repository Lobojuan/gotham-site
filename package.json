{
  "name": "gotham-site",
  "version": "1.0.0",
  "private": true,
  "description": "Gotham Consult — static site, served by a zero-dependency Node static server",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs"
  }
}
